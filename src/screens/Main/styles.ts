import {StyleSheet} from 'react-native';

const MainStyles = StyleSheet.create({
  container: {
    padding: 20,
  },
  title: {
    fontSize: 32,
    fontWeight: 'bold',
    marginBottom: 10,
    color: '#010048',
  },
});

export default MainStyles;
